# Copy this file to `.env` and fill in your Supabase project credentials.
# The URL should look like `https://<your-project-ref>.supabase.co` and the
# anon key can be found in the Supabase dashboard under Settings → API → `anon` key.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=